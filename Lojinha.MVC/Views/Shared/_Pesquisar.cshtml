@using (Html.BeginForm("Pesquisar", Html.Controller(), FormMethod.Get))
{
    <div class="row" id="search-button">
        <div class="col-lg-12">
            <div class="input-group">
                <input type="text" name="filtro" placeholder="Search for..." class="form-control" />
                <span class="input-group-btn">
                    <button type="submit" class="btn btn-default">Buscar!</button>
                </span>
            </div>
        </div>
    </div>
}