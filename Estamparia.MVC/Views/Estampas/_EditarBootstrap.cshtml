@model Estamparia.MVC.Models.Estampa

@{
    ViewBag.Title = "Editar";
}

<h1 class="display-4 mt-3">Editar</h1>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <div>
        <h4>Estampa</h4>

        @Html.HiddenFor(model => model.EstampaId)
		@Html.Partial("_CriarOuEditarBootstrap", Model)

        <p>
            <input class="btn btn-primary active" type="submit" value="Salvar" />
        </p>
    </div>
}

<div>
    @Html.ActionLink("Voltar para Listagem", "Indice")
</div>
