@model Estamparia.MVC.Models.Categoria

@{
    ViewBag.Title = "Criar";
}

@{
    string layoutValue = Request.Cookies.Get("LayoutName").Value;

    switch (layoutValue)
    {
        case "Bootstrap":
            Html.RenderPartial("Bootstrap/_Criar", Model);
            break;

        case "Ink":
            Html.RenderPartial("Ink/_Criar", Model);
            break;

        case "Zurb":
            Html.RenderPartial("Zurb/_Criar", Model);
            break;

    }
}